<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Baumann Estate · AI Property Advisor</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400;1,600&family=Outfit:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
  :root {
    --purple-deep:   #1E0B35;
    --purple-dark:   #2D1550;
    --purple-mid:    #3B1F5E;
    --purple-card:   #4A2872;
    --purple-light:  #6B4488;
    --purple-bright: #9B59D4;
    --purple-accent: #B07FE0;
    --purple-pale:   #D4B8F0;
    --white:         #FFFFFF;
    --off-white:     #F5F1FB;
    --gray-light:    #C8B8DC;
    --gray-mid:      #8A709A;
    --border:        rgba(155,89,212,0.2);
    --border-bright: rgba(155,89,212,0.5);
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    background: var(--purple-deep);
    color: var(--white);
    font-family: 'Outfit', sans-serif;
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* ── BACKGROUND TEXTURE ─────────────────────────── */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background:
      radial-gradient(ellipse 80% 50% at 10% 20%, rgba(155,89,212,0.12) 0%, transparent 60%),
      radial-gradient(ellipse 60% 40% at 90% 80%, rgba(59,31,94,0.4) 0%, transparent 60%);
    pointer-events: none;
    z-index: 0;
  }

  /* ── NAV ────────────────────────────────────────── */
  nav {
    position: relative;
    z-index: 10;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 20px 48px;
    border-bottom: 1px solid var(--border);
    background: rgba(30,11,53,0.8);
    backdrop-filter: blur(16px);
  }

  .logo-wrap {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .logo-name {
    font-family: 'Cormorant Garamond', serif;
    font-size: 22px;
    font-weight: 600;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--white);
    line-height: 1;
  }

  .logo-sub {
    font-size: 9px;
    letter-spacing: 4px;
    text-transform: uppercase;
    color: var(--purple-accent);
    font-weight: 500;
  }

  .logo-line {
    width: 100%;
    height: 1px;
    background: linear-gradient(90deg, var(--purple-bright), transparent);
    margin-top: 3px;
  }

  .nav-right {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .nav-tag {
    font-size: 10px;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--gray-mid);
    font-weight: 500;
  }

  /* ── LANGUAGE TOGGLE ────────────────────────────── */
  .lang-toggle {
    display: flex;
    border: 1px solid var(--border-bright);
    overflow: hidden;
    border-radius: 2px;
  }

  .lang-btn {
    padding: 7px 18px;
    font-size: 11px;
    font-family: 'Outfit', sans-serif;
    font-weight: 600;
    letter-spacing: 2px;
    text-transform: uppercase;
    background: transparent;
    border: none;
    color: var(--gray-mid);
    cursor: pointer;
    transition: all 0.25s;
  }

  .lang-btn.active {
    background: var(--purple-bright);
    color: var(--white);
  }

  .lang-btn:hover:not(.active) {
    background: var(--border);
    color: var(--purple-pale);
  }

  /* ── HERO ───────────────────────────────────────── */
  .hero {
    position: relative;
    z-index: 1;
    padding: 64px 48px 48px;
    border-bottom: 1px solid var(--border);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 48px;
    align-items: center;
  }

  .hero-label {
    font-size: 10px;
    letter-spacing: 4px;
    text-transform: uppercase;
    color: var(--purple-bright);
    font-weight: 500;
    margin-bottom: 16px;
  }

  .hero h1 {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(36px, 4.5vw, 58px);
    font-weight: 300;
    line-height: 1.08;
    margin-bottom: 20px;
  }

  .hero h1 em {
    font-style: italic;
    color: var(--purple-pale);
  }

  .hero-desc {
    font-size: 14px;
    color: var(--gray-light);
    line-height: 1.75;
    font-weight: 300;
    max-width: 440px;
  }

  .hero-stats {
    display: flex;
    flex-direction: column;
    gap: 1px;
    border: 1px solid var(--border);
  }

  .stat-row {
    display: flex;
    align-items: center;
    gap: 24px;
    padding: 24px 28px;
    background: rgba(74,40,114,0.3);
    border-bottom: 1px solid var(--border);
    transition: background 0.2s;
  }

  .stat-row:last-child { border-bottom: none; }
  .stat-row:hover { background: rgba(74,40,114,0.5); }

  .stat-icon {
    font-size: 24px;
    width: 36px;
    text-align: center;
    flex-shrink: 0;
  }

  .stat-num {
    font-family: 'Cormorant Garamond', serif;
    font-size: 32px;
    font-weight: 600;
    color: var(--purple-pale);
    line-height: 1;
    min-width: 80px;
  }

  .stat-label {
    font-size: 12px;
    color: var(--gray-mid);
    font-weight: 300;
    line-height: 1.4;
  }

  /* ── AGENTS GRID ────────────────────────────────── */
  .agents-section {
    position: relative;
    z-index: 1;
    padding: 48px;
    border-bottom: 1px solid var(--border);
  }

  .section-header {
    display: flex;
    align-items: baseline;
    gap: 16px;
    margin-bottom: 32px;
  }

  .section-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: 28px;
    font-weight: 400;
    color: var(--white);
  }

  .section-num {
    font-size: 10px;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--purple-accent);
    font-weight: 500;
  }

  .agents-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1px;
    background: var(--border);
    border: 1px solid var(--border);
  }

  .agent-card {
    background: var(--purple-deep);
    padding: 28px 24px;
    cursor: pointer;
    transition: background 0.25s;
    position: relative;
    overflow: hidden;
  }

  .agent-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 3px;
    background: var(--purple-bright);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.3s;
  }

  .agent-card:hover::before,
  .agent-card.active::before {
    transform: scaleX(1);
  }

  .agent-card:hover,
  .agent-card.active {
    background: var(--purple-dark);
  }

  .agent-num {
    font-size: 9px;
    letter-spacing: 3px;
    color: var(--purple-accent);
    font-weight: 600;
    margin-bottom: 12px;
  }

  .agent-name {
    font-family: 'Cormorant Garamond', serif;
    font-size: 18px;
    font-weight: 600;
    color: var(--white);
    margin-bottom: 6px;
    line-height: 1.2;
  }

  .agent-sub {
    font-size: 10px;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: var(--purple-accent);
    margin-bottom: 14px;
    font-weight: 500;
  }

  .agent-desc {
    font-size: 12px;
    color: var(--gray-mid);
    line-height: 1.65;
    font-weight: 300;
  }

  .agent-badge {
    display: inline-block;
    margin-top: 16px;
    font-size: 9px;
    letter-spacing: 2px;
    text-transform: uppercase;
    padding: 4px 10px;
    font-weight: 600;
  }

  .badge-live {
    background: rgba(155,89,212,0.2);
    border: 1px solid var(--purple-bright);
    color: var(--purple-pale);
    animation: glow 2s infinite;
  }

  .badge-soon {
    border: 1px solid var(--border);
    color: var(--gray-mid);
  }

  @keyframes glow {
    0%, 100% { box-shadow: 0 0 0px var(--purple-bright); }
    50% { box-shadow: 0 0 8px rgba(155,89,212,0.4); }
  }

  /* ── DEMO SECTION ───────────────────────────────── */
  .demo-section {
    position: relative;
    z-index: 1;
    padding: 48px;
  }

  .demo-intro {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 32px;
    margin-bottom: 32px;
    flex-wrap: wrap;
  }

  .demo-intro-left h2 {
    font-family: 'Cormorant Garamond', serif;
    font-size: 32px;
    font-weight: 400;
    color: var(--white);
    margin-bottom: 8px;
  }

  .demo-intro-left h2 em {
    font-style: italic;
    color: var(--purple-pale);
  }

  .demo-note {
    font-size: 12px;
    color: var(--gray-mid);
    max-width: 280px;
    line-height: 1.7;
    text-align: right;
  }

  /* ── CHAT ───────────────────────────────────────── */
  .chat-wrap {
    border: 1px solid var(--border-bright);
    display: flex;
    flex-direction: column;
    height: 560px;
    background: rgba(45,21,80,0.6);
    backdrop-filter: blur(12px);
    position: relative;
    overflow: hidden;
  }

  /* decorative corner */
  .chat-wrap::after {
    content: '';
    position: absolute;
    top: 0; right: 0;
    width: 120px; height: 120px;
    background: radial-gradient(circle at top right, rgba(155,89,212,0.08), transparent 70%);
    pointer-events: none;
  }

  .chat-topbar {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 14px 20px;
    border-bottom: 1px solid var(--border);
    background: rgba(30,11,53,0.6);
    flex-shrink: 0;
  }

  .chat-status-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: #7CFC00;
    flex-shrink: 0;
    box-shadow: 0 0 6px rgba(124,252,0,0.5);
    animation: pulse-green 2s infinite;
  }

  @keyframes pulse-green {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  .chat-topbar-title {
    font-size: 13px;
    font-weight: 500;
    color: var(--white);
    flex: 1;
  }

  .chat-topbar-sub {
    font-size: 10px;
    color: var(--gray-mid);
    letter-spacing: 1px;
  }

  .chat-messages {
    flex: 1;
    overflow-y: auto;
    padding: 24px 20px;
    display: flex;
    flex-direction: column;
    gap: 14px;
    scrollbar-width: thin;
    scrollbar-color: var(--border) transparent;
  }

  .chat-messages::-webkit-scrollbar { width: 3px; }
  .chat-messages::-webkit-scrollbar-thumb { background: var(--purple-card); }

  /* ── MESSAGES ───────────────────────────────────── */
  .msg {
    display: flex;
    gap: 10px;
    max-width: 82%;
    animation: msgIn 0.3s cubic-bezier(0.34,1.56,0.64,1);
  }

  @keyframes msgIn {
    from { opacity: 0; transform: translateY(10px) scale(0.97); }
    to   { opacity: 1; transform: translateY(0) scale(1); }
  }

  .msg.user { align-self: flex-end; flex-direction: row-reverse; }

  .msg-av {
    width: 30px; height: 30px;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 13px;
    flex-shrink: 0;
    border: 1px solid var(--border);
    background: rgba(74,40,114,0.4);
  }

  .msg.user .msg-av { background: rgba(155,89,212,0.15); }

  .msg-bubble {
    padding: 11px 15px;
    font-size: 13px;
    line-height: 1.65;
    font-weight: 300;
    position: relative;
  }

  .msg.agent .msg-bubble {
    background: rgba(59,31,94,0.7);
    border: 1px solid var(--border);
    color: rgba(255,255,255,0.88);
    border-radius: 0 8px 8px 8px;
  }

  .msg.user .msg-bubble {
    background: rgba(155,89,212,0.2);
    border: 1px solid var(--border-bright);
    color: var(--white);
    border-radius: 8px 0 8px 8px;
  }

  .msg-bubble strong { color: var(--purple-pale); font-weight: 500; }

  /* ── QUICK REPLIES ──────────────────────────────── */
  .quick-replies {
    padding: 10px 20px;
    border-top: 1px solid var(--border);
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    background: rgba(30,11,53,0.4);
    flex-shrink: 0;
  }

  .quick-btn {
    font-size: 11px;
    padding: 6px 14px;
    border: 1px solid var(--border);
    background: transparent;
    color: var(--gray-light);
    cursor: pointer;
    font-family: 'Outfit', sans-serif;
    font-weight: 400;
    transition: all 0.2s;
    border-radius: 2px;
    white-space: nowrap;
  }

  .quick-btn:hover {
    border-color: var(--purple-bright);
    color: var(--purple-pale);
    background: rgba(155,89,212,0.08);
  }

  /* ── INPUT ──────────────────────────────────────── */
  .chat-input-row {
    display: flex;
    gap: 10px;
    padding: 14px 20px;
    border-top: 1px solid var(--border);
    background: rgba(30,11,53,0.6);
    flex-shrink: 0;
  }

  .chat-input {
    flex: 1;
    background: rgba(74,40,114,0.25);
    border: 1px solid var(--border);
    color: var(--white);
    padding: 10px 16px;
    font-size: 13px;
    font-family: 'Outfit', sans-serif;
    font-weight: 300;
    outline: none;
    transition: border-color 0.2s;
    border-radius: 2px;
  }

  .chat-input:focus { border-color: var(--purple-bright); }
  .chat-input::placeholder { color: var(--gray-mid); }

  .send-btn {
    background: var(--purple-bright);
    border: none;
    color: var(--white);
    padding: 10px 22px;
    font-size: 11px;
    font-family: 'Outfit', sans-serif;
    font-weight: 600;
    letter-spacing: 2px;
    text-transform: uppercase;
    cursor: pointer;
    transition: background 0.2s;
    border-radius: 2px;
  }

  .send-btn:hover { background: var(--purple-accent); }
  .send-btn:disabled { opacity: 0.4; cursor: not-allowed; }

  /* ── TYPING ─────────────────────────────────────── */
  .typing {
    display: flex;
    align-items: center;
    gap: 5px;
    padding: 11px 15px;
    background: rgba(59,31,94,0.7);
    border: 1px solid var(--border);
    width: fit-content;
    border-radius: 0 8px 8px 8px;
  }

  .tdot {
    width: 6px; height: 6px;
    border-radius: 50%;
    background: var(--purple-accent);
    animation: bounce 1.2s infinite;
  }
  .tdot:nth-child(2) { animation-delay: 0.2s; }
  .tdot:nth-child(3) { animation-delay: 0.4s; }

  @keyframes bounce {
    0%,60%,100% { transform: translateY(0); opacity: 0.4; }
    30% { transform: translateY(-6px); opacity: 1; }
  }

  /* ── IMPACT BAR ─────────────────────────────────── */
  .impact-bar {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    background: var(--purple-mid);
    border-top: 1px solid var(--border-bright);
  }

  .impact-item {
    padding: 28px 24px;
    border-right: 1px solid rgba(255,255,255,0.08);
    text-align: center;
  }

  .impact-item:last-child { border-right: none; }

  .impact-num {
    font-family: 'Cormorant Garamond', serif;
    font-size: 36px;
    font-weight: 600;
    color: var(--purple-pale);
    line-height: 1;
    margin-bottom: 6px;
  }

  .impact-label {
    font-size: 11px;
    color: var(--gray-light);
    font-weight: 300;
    line-height: 1.4;
  }

  /* ── FOOTER ─────────────────────────────────────── */
  footer {
    position: relative;
    z-index: 1;
    padding: 24px 48px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-top: 1px solid var(--border);
    background: rgba(30,11,53,0.6);
    flex-wrap: wrap;
    gap: 12px;
  }

  footer .foot-logo {
    font-family: 'Cormorant Garamond', serif;
    font-size: 14px;
    letter-spacing: 3px;
    color: var(--purple-pale);
    text-transform: uppercase;
  }

  footer .foot-contact {
    font-size: 11px;
    color: var(--gray-mid);
    text-align: center;
    line-height: 1.6;
  }

  footer .foot-tag {
    font-size: 10px;
    color: var(--purple-accent);
    letter-spacing: 2px;
    text-transform: uppercase;
  }

  /* ── RESPONSIVE ─────────────────────────────────── */
  @media (max-width: 900px) {
    nav, .hero, .agents-section, .demo-section, footer { padding-left: 24px; padding-right: 24px; }
    .hero { grid-template-columns: 1fr; }
    .agents-grid { grid-template-columns: repeat(2, 1fr); }
    .impact-bar { grid-template-columns: repeat(2, 1fr); }
    .demo-note { text-align: left; }
    .nav-tag { display: none; }
  }

  @media (max-width: 540px) {
    .agents-grid { grid-template-columns: 1fr; }
    .impact-bar { grid-template-columns: 1fr 1fr; }
  }

  /* ── TRANSITIONS ────────────────────────────────── */
  [data-en], [data-de] { transition: opacity 0.2s; }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <div class="logo-wrap">
    <div class="logo-name">Baumann Estate</div>
    <div class="logo-line"></div>
    <div class="logo-sub">AG · Zürichsee</div>
  </div>
  <div class="nav-right">
    <span class="nav-tag" data-en="AI Agents · Demo" data-de="KI-Agenten · Demo">AI Agents · Demo</span>
    <div class="lang-toggle">
      <button class="lang-btn active" onclick="setLang('en')">EN</button>
      <button class="lang-btn" onclick="setLang('de')">DE</button>
    </div>
  </div>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-left">
    <div class="hero-label" data-en="EXCLUSIVE AI STRATEGY · 2025" data-de="KI-STRATEGIE · 2025">EXCLUSIVE AI STRATEGY · 2025</div>
    <h1 data-en="Artificial Intelligence<br>for <em>Baumann Estate.</em>" data-de="Künstliche Intelligenz<br>für <em>Baumann Estate.</em>">
      Artificial Intelligence<br>for <em>Baumann Estate.</em>
    </h1>
    <p class="hero-desc" data-en="Four AI agents designed to elevate client experience, qualify leads 24/7, and give Ingila & Claudio Baumann more time for what truly matters — building relationships." data-de="Vier KI-Agenten, die das Kundenerlebnis verbessern, Leads rund um die Uhr qualifizieren und Ingila & Claudio Baumann mehr Zeit für das Wesentliche geben — persönliche Beziehungen.">
      Four AI agents designed to elevate client experience, qualify leads 24/7, and give Ingila & Claudio Baumann more time for what truly matters — building relationships.
    </p>
  </div>
  <div class="hero-stats">
    <div class="stat-row">
      <div class="stat-icon">⭐</div>
      <div class="stat-num">5.0</div>
      <div class="stat-label" data-en="Google Rating<br>Top-rated certified" data-de="Google-Bewertung<br>Top-bewertet zertifiziert">Google Rating<br>Top-rated certified</div>
    </div>
    <div class="stat-row">
      <div class="stat-icon">🏗</div>
      <div class="stat-num">16+</div>
      <div class="stat-label" data-en="New developments<br>successfully marketed" data-de="Neubauprojekte<br>erfolgreich vermarktet">New developments<br>successfully marketed</div>
    </div>
    <div class="stat-row">
      <div class="stat-icon">🏠</div>
      <div class="stat-num">110+</div>
      <div class="stat-label" data-en="Residential units<br>sold at Zürichsee" data-de="Wohneinheiten<br>am Zürichsee verkauft">Residential units<br>sold at Zürichsee</div>
    </div>
    <div class="stat-row">
      <div class="stat-icon">🌍</div>
      <div class="stat-num">70+</div>
      <div class="stat-label" data-en="Countries via LeadingRE<br>& Luxury Portfolio" data-de="Länder über LeadingRE<br>& Luxury Portfolio">Countries via LeadingRE<br>& Luxury Portfolio</div>
    </div>
  </div>
</section>

<!-- AGENTS OVERVIEW -->
<section class="agents-section">
  <div class="section-header">
    <span class="section-num">01</span>
    <h2 class="section-title" data-en="The Four Agents" data-de="Die vier Agenten">The Four Agents</h2>
  </div>
  <div class="agents-grid">
    <div class="agent-card active" onclick="selectAgent(0, this)">
      <div class="agent-num">01</div>
      <div class="agent-name" data-en="Property Advisor" data-de="Immobilien-Berater">Property Advisor</div>
      <div class="agent-sub" data-en="24/7 Client Concierge" data-de="24/7 Kundenbetreuung">24/7 Client Concierge</div>
      <div class="agent-desc" data-en="Answers buyer enquiries around the clock in DE, EN & ES. Qualifies leads and schedules viewings automatically." data-de="Beantwortet Käuferanfragen rund um die Uhr auf DE, EN & ES. Qualifiziert Leads und vereinbart automatisch Besichtigungen.">
        Answers buyer enquiries around the clock in DE, EN & ES. Qualifies leads and schedules viewings automatically.
      </div>
      <span class="agent-badge badge-live" data-en="● LIVE DEMO" data-de="● LIVE DEMO">● LIVE DEMO</span>
    </div>
    <div class="agent-card" onclick="selectAgent(1, this)">
      <div class="agent-num">02</div>
      <div class="agent-name" data-en="Listing Analyst" data-de="Listing-Analyst">Listing Analyst</div>
      <div class="agent-sub" data-en="Property Intelligence" data-de="Immobilien-Intelligenz">Property Intelligence</div>
      <div class="agent-desc" data-en="Auto-generates premium property listings in DE/EN, estimates market price, and highlights unique selling points." data-de="Erstellt automatisch Premium-Exposés auf DE/EN, schätzt Marktpreise und hebt Alleinstellungsmerkmale hervor.">
        Auto-generates premium property listings in DE/EN, estimates market price, and highlights unique selling points.
      </div>
      <span class="agent-badge badge-soon" data-en="COMING SOON" data-de="DEMNÄCHST">COMING SOON</span>
    </div>
    <div class="agent-card" onclick="selectAgent(2, this)">
      <div class="agent-num">03</div>
      <div class="agent-name" data-en="Outreach Agent" data-de="Outreach-Agent">Outreach Agent</div>
      <div class="agent-sub" data-en="Personalised Follow-Up" data-de="Persönliche Nachverfolgung">Personalised Follow-Up</div>
      <div class="agent-desc" data-en="Drafts personalised emails per lead profile. Automated follow-up at day 3, 7 and 14 in perfect Baumann brand voice." data-de="Verfasst individuelle E-Mails pro Lead-Profil. Automatische Nachverfolgung nach Tag 3, 7 und 14 in der Baumann-Markenstimme.">
        Drafts personalised emails per lead profile. Automated follow-up at day 3, 7 and 14 in perfect Baumann brand voice.
      </div>
      <span class="agent-badge badge-soon" data-en="COMING SOON" data-de="DEMNÄCHST">COMING SOON</span>
    </div>
    <div class="agent-card" onclick="selectAgent(3, this)">
      <div class="agent-num">04</div>
      <div class="agent-name" data-en="Market Intel" data-de="Marktanalyse">Market Intel</div>
      <div class="agent-sub" data-en="Market Intelligence" data-de="Marktintelligenz">Market Intelligence</div>
      <div class="agent-desc" data-en="Monitors Zürichsee prices daily, tracks competitor listings, and delivers a weekly market intelligence report." data-de="Überwacht Zürichsee-Preise täglich, verfolgt Konkurrenzangebote und liefert wöchentliche Marktberichte.">
        Monitors Zürichsee prices daily, tracks competitor listings, and delivers a weekly market intelligence report.
      </div>
      <span class="agent-badge badge-soon" data-en="COMING SOON" data-de="DEMNÄCHST">COMING SOON</span>
    </div>
  </div>
</section>

<!-- DEMO -->
<section class="demo-section">
  <div class="demo-intro">
    <div class="demo-intro-left">
      <span class="section-num">02</span>
      <h2 data-en="Property Advisor<br><em>in action</em>" data-de="Immobilien-Berater<br><em>in Aktion</em>">
        Property Advisor<br><em>in action</em>
      </h2>
    </div>
    <p class="demo-note" data-en="This agent is powered by real AI. Type any question as if you were a buyer or seller interested in Baumann Estate — in English or German." data-de="Dieser Agent wird von echter KI betrieben. Stellen Sie eine Frage wie ein Käufer oder Verkäufer, der an Baumann Estate interessiert ist — auf Englisch oder Deutsch.">
      This agent is powered by real AI. Type any question as if you were a buyer or seller interested in Baumann Estate — in English or German.
    </p>
  </div>

  <div class="chat-wrap">
    <div class="chat-topbar">
      <div class="chat-status-dot"></div>
      <div class="chat-topbar-title" data-en="Baumann Estate · Property Advisor" data-de="Baumann Estate · Immobilien-Berater">Baumann Estate · Property Advisor</div>
      <div class="chat-topbar-sub" data-en="Powered by AI · DE / EN / ES" data-de="KI-gestützt · DE / EN / ES">Powered by AI · DE / EN / ES</div>
    </div>

    <div class="chat-messages" id="chatMessages">
      <div class="msg agent" id="welcomeMsg">
        <div class="msg-av">🏠</div>
        <div class="msg-bubble" id="welcomeText">
          Good day. I'm the Baumann Estate Property Advisor — your personal guide to exclusive real estate on the <strong>Zürichsee</strong>.<br><br>
          Whether you're looking to buy a lakeside residence, explore a new development, or simply understand the current market — I'm here. How can I assist you today?
        </div>
      </div>
    </div>

    <div class="quick-replies" id="quickReplies">
      <button class="quick-btn" onclick="sendQuick(this)" data-en="What properties are available?" data-de="Welche Immobilien sind verfügbar?">What properties are available?</button>
      <button class="quick-btn" onclick="sendQuick(this)" data-en="What are prices like near the lake?" data-de="Wie sind die Preise am See?">What are prices like near the lake?</button>
      <button class="quick-btn" onclick="sendQuick(this)" data-en="Can I invest from abroad?" data-de="Kann ich vom Ausland aus investieren?">Can I invest from abroad?</button>
      <button class="quick-btn" onclick="sendQuick(this)" data-en="How does the buying process work?" data-de="Wie funktioniert der Kaufprozess?">How does the buying process work?</button>
    </div>

    <div class="chat-input-row">
      <input class="chat-input" id="chatInput" type="text"
        placeholder="Ask anything about Baumann Estate..."
        onkeydown="if(event.key==='Enter') sendMessage()">
      <button class="send-btn" id="sendBtn" onclick="sendMessage()" data-en="Send" data-de="Senden">Send</button>
    </div>
  </div>
</section>

<!-- IMPACT BAR -->
<div class="impact-bar">
  <div class="impact-item">
    <div class="impact-num">24/7</div>
    <div class="impact-label" data-en="Availability<br>No missed enquiries" data-de="Verfügbarkeit<br>Keine verpassten Anfragen">Availability<br>No missed enquiries</div>
  </div>
  <div class="impact-item">
    <div class="impact-num">3×</div>
    <div class="impact-label" data-en="More qualified leads<br>through instant response" data-de="Mehr qualifizierte Leads<br>durch Sofortreaktion">More qualified leads<br>through instant response</div>
  </div>
  <div class="impact-item">
    <div class="impact-num">−80%</div>
    <div class="impact-label" data-en="Time on standard<br>enquiries & follow-ups" data-de="Zeit für Standard-<br>anfragen & Nachverfolgung">Time on standard<br>enquiries & follow-ups</div>
  </div>
  <div class="impact-item">
    <div class="impact-num">CHF 0</div>
    <div class="impact-label" data-en="Extra headcount<br>for high enquiry volumes" data-de="Zusätzliches Personal<br>für hohe Anfragenmengen">Extra headcount<br>for high enquiry volumes</div>
  </div>
</div>

<!-- FOOTER -->
<footer>
  <div class="foot-logo">Baumann Estate AG</div>
  <div class="foot-contact">
    Bönirainstrasse 14/16 · 8800 Thalwil · +41 44 301 50 00<br>
    contact@baumann-estate.ch · baumann-estate.ch
  </div>
  <div class="foot-tag" data-en="AI Strategy · 2025" data-de="KI-Strategie · 2025">AI Strategy · 2025</div>
</footer>

<script>
// ── LANGUAGE SYSTEM ──────────────────────────────────────────────
let currentLang = 'en';

const WELCOME = {
  en: `Good day. I'm the Baumann Estate Property Advisor — your personal guide to exclusive real estate on the <strong>Zürichsee</strong>.<br><br>Whether you're looking to buy a lakeside residence, explore a new development, or simply understand the current market — I'm here. How can I assist you today?`,
  de: `Guten Tag. Ich bin der Baumann Estate Immobilien-Berater — Ihr persönlicher Begleiter für exklusive Immobilien am <strong>Zürichsee</strong>.<br><br>Ob Sie eine Seevilla kaufen, ein Neubauprojekt erkunden oder den aktuellen Markt verstehen möchten — ich bin für Sie da. Wie kann ich Ihnen heute helfen?`
};

const PLACEHOLDERS = {
  en: 'Ask anything about Baumann Estate...',
  de: 'Stellen Sie eine Frage zu Baumann Estate...'
};

function setLang(lang) {
  currentLang = lang;

  // Toggle button states
  document.querySelectorAll('.lang-btn').forEach(btn => {
    btn.classList.toggle('active', btn.textContent.toLowerCase() === lang);
  });

  // Update all [data-en] / [data-de] elements
  document.querySelectorAll('[data-en]').forEach(el => {
    const text = el.getAttribute(`data-${lang}`);
    if (!text) return;
    if (el.tagName === 'INPUT') {
      el.placeholder = text;
    } else if (text.includes('<')) {
      el.innerHTML = text;
    } else {
      el.textContent = text;
    }
  });

  // Update chat input placeholder
  document.getElementById('chatInput').placeholder = PLACEHOLDERS[lang];

  // Update quick reply button text
  document.querySelectorAll('.quick-btn').forEach(btn => {
    const t = btn.getAttribute(`data-${lang}`);
    if (t) btn.textContent = t;
  });

  // Update send button
  document.getElementById('sendBtn').textContent = lang === 'en' ? 'Send' : 'Senden';

  // Update welcome message if it's still the default
  const welcomeEl = document.getElementById('welcomeText');
  if (welcomeEl) welcomeEl.innerHTML = WELCOME[lang];
}

// ── AGENT SELECT ─────────────────────────────────────────────────
function selectAgent(index, el) {
  document.querySelectorAll('.agent-card').forEach(c => c.classList.remove('active'));
  el.classList.add('active');
}

// ── CHAT SYSTEM ──────────────────────────────────────────────────
const SYSTEM_PROMPT = `You are the official AI Property Advisor for Baumann Estate AG, a premium real estate agency based in Thalwil, Zurich, specialising in exclusive residential properties and new development projects around the Zürichsee (Lake Zurich).

About Baumann Estate:
- Founded and run personally by Ingila & Claudio Baumann
- Located at Bönirainstrasse 14/16, 8800 Thalwil
- Phone: +41 44 301 50 00 | Email: contact@baumann-estate.ch
- 5.0 Google rating — top-rated certified
- 16+ new development projects completed and marketed
- 110+ residential units successfully sold
- Member of Leading Real Estate Companies of the World® and Luxury Portfolio International (70+ countries)
- 360° advisory approach covering purchase, sale, valuation, taxes and legal matters

Current portfolio highlights:
- Panorama, Kilchberg — panoramic lake views
- Chalet Mountain View, Davos Wiesen — alpine luxury chalet
- GRACE — 5.5-room apartment, 8055 Zürich (Binz district)
- Garden10, Rüschlikon — 6.5-room house
- Lakeside Serenity, Horgen — directly on the lake
- NOBLE — apartment in 8952 Schlieren

Your role:
- Welcome potential buyers and sellers warmly and professionally
- Qualify their needs, budget, timeline and property preferences
- Explain Baumann Estate's 360° advisory approach
- Answer questions about the Swiss real estate market, buying process, taxes, legal aspects, and financing
- Invite prospects to schedule a personal consultation with Ingila or Claudio
- Always maintain the tone of a luxury, personalised boutique firm — never corporate, always human
- Respond in whatever language the user writes in (German, English, or Spanish)
- Never invent specific prices — explain that each property is priced individually and encourage direct contact
- Keep responses concise (2–4 short paragraphs), warm, professional
- Always end with a gentle next step, question, or invitation to connect`;

let history = [];
let busy = false;

function addMsg(role, html) {
  const wrap = document.getElementById('chatMessages');
  const div = document.createElement('div');
  div.className = `msg ${role}`;
  div.innerHTML = `
    <div class="msg-av">${role === 'user' ? '👤' : '🏠'}</div>
    <div class="msg-bubble">${html}</div>
  `;
  wrap.appendChild(div);
  wrap.scrollTop = wrap.scrollHeight;
}

function showTyping() {
  const wrap = document.getElementById('chatMessages');
  const div = document.createElement('div');
  div.className = 'msg agent';
  div.id = 'typing';
  div.innerHTML = `<div class="msg-av">🏠</div><div class="typing"><div class="tdot"></div><div class="tdot"></div><div class="tdot"></div></div>`;
  wrap.appendChild(div);
  wrap.scrollTop = wrap.scrollHeight;
}

function removeTyping() {
  const t = document.getElementById('typing');
  if (t) t.remove();
}

async function sendMessage() {
  if (busy) return;
  const inp = document.getElementById('chatInput');
  const text = inp.value.trim();
  if (!text) return;

  inp.value = '';
  document.getElementById('quickReplies').style.display = 'none';
  addMsg('user', text);
  history.push({ role: 'user', content: text });

  busy = true;
  document.getElementById('sendBtn').disabled = true;
  showTyping();

  try {
    const res = await fetch('https://api.anthropic.com/v1/messages', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        model: 'claude-sonnet-4-20250514',
        max_tokens: 1000,
        system: SYSTEM_PROMPT,
        messages: history
      })
    });

    const data = await res.json();
    removeTyping();

    const reply = data.content?.[0]?.text || (currentLang === 'de'
      ? 'Entschuldigung, ein technisches Problem ist aufgetreten. Bitte kontaktieren Sie uns direkt unter contact@baumann-estate.ch.'
      : 'I apologise for the technical difficulty. Please contact us directly at contact@baumann-estate.ch.');

    history.push({ role: 'assistant', content: reply });
    addMsg('agent', reply.replace(/\n/g, '<br>').replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>'));

  } catch (e) {
    removeTyping();
    const errMsg = currentLang === 'de'
      ? 'Entschuldigung, ein Fehler ist aufgetreten. Bitte rufen Sie uns an: <strong>+41 44 301 50 00</strong>'
      : 'I apologise for the error. Please call us directly: <strong>+41 44 301 50 00</strong>';
    addMsg('agent', errMsg);
  }

  busy = false;
  document.getElementById('sendBtn').disabled = false;
}

function sendQuick(btn) {
  document.getElementById('chatInput').value = btn.textContent;
  sendMessage();
}
</script>
</body>
</html>
